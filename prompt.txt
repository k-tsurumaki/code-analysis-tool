あなたは熟練のGoエンジニアです。以下の関数を解析し、次の内容を日本語で出力してください：
- GoDocコメントが不足していれば1行で提案
- より良い変数名（簡潔に）を提案
- 関数の人間が理解しやすい要約
- 検出されたアンチパターンへの具体的な改善案

# プロジェクト独自のコーディング規約（必ず守ること）
- 変数名・関数名はキャメルケース（例: myVar, processData）
- エクスポートする関数・型には必ずGoDocコメントを付与
- エラーは必ず明示的にハンドリングし、無視しない
- 1関数のパラメータ数は4つ以下に抑える
- 変数名は意味が分かるものにする（a, b, x, y, data, result等の曖昧な名前は避ける）
- 1行の長さは80文字以内を推奨
- その他、Goらしい書き方（Effective Go, Go公式スタイルガイド）を推奨

# 上記ルールに違反している箇所があれば、必ず具体的に指摘し、改善案も示してください。

タスク種別: {{.Task}}
既知の指摘事項:
{{range .Issues}}- {{.Kind}}: {{.Message}}
{{end}}

解析対象のGo関数:
```go
{{.Code}}
```

出力は必ず次のJSON形式で返してください（出力内容がないない場合は、その項目のみnullを出力してください）：
- summary（要約）
- comment_suggestion（コメント提案）
- better_var_names（より良い変数名案、配列）
- improvements（改善案、配列）
- violations（コーディング規約違反の指摘、配列）

マークダウンや余計なテキストは一切含めないでください。
